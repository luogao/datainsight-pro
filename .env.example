# DataInsight Pro v2.0 - 环境配置示例
#
# 注意：V2 版本不再需要 PANDAAI_API_KEY
# PandaAI 库通过 LLM (OpenAI API) 工作，只需要配置 LLM 相关的环境变量

# ========================================
# 必填配置
# ========================================

# LLM API 密钥（CrewAI 和 PandaAI 共用）
# PandaAI 库使用 LLM 进行数据分析，所以只需要这个 API Key
OPENAI_API_KEY=your_openai_api_key_here

# ========================================
# 可选配置（自定义 LLM 服务）
# ========================================

# LLM API 端点（默认：OpenAI 官方 API）
# 如果使用其他兼容 OpenAI API 的服务，修改此项
OPENAI_BASE_URL=https://api.openai.com/v1

# LLM 模型名称（默认：gpt-4）
# 可以根据需要修改为其他模型
OPENAI_MODEL=gpt-4

# ========================================
# 其他兼容服务的配置示例
# ========================================

# 使用 DeepSeek（推荐，性价比高）
# OPENAI_API_KEY=your_deepseek_api_key
# OPENAI_BASE_URL=https://api.deepseek.com/v1
# OPENAI_MODEL=deepseek-chat

# 使用 Azure OpenAI
# OPENAI_API_KEY=your_azure_api_key
# OPENAI_BASE_URL=https://your-resource.openai.azure.com/openai/deployments/your-deployment
# OPENAI_MODEL=gpt-4

# 使用本地模型（如 llama-cpp-server）
# OPENAI_API_KEY=dummy_key
# OPENAI_BASE_URL=http://localhost:8080/v1
# OPENAI_MODEL=local-model

# ========================================
# 说明
# ========================================
#
# V1 版本的 PANDAAI_API_KEY 已废弃，不再需要。
# 原因：PandaAI 库本身不提供 API，而是通过 LLM 进行数据分析。
# 所以配置 LLM 的 API Key 即可。
#
# PandaAI 的工作原理：
# 1. 接收 DataFrame 和自然语言问题
# 2. 通过 LLM 理解问题
# 3. 生成 Python 代码来分析数据
# 4. 执行代码并返回结果
#
# 因此，只需要 LLM (OpenAI API) 的 API Key 就可以工作。
